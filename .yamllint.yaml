extends: default

rules:
  empty-lines:
    max: 1  # allow up to 1 blank line (matches yamlfix whitelines = 1)
    max-start: 0
    max-end: 0
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ['true', 'false']
    check-keys: false
  document-start:
    present: false
