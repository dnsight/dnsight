# Python API

The public SDK surface is exposed from the **`dnsight`** package (re-exported symbols) and supporting modules under **`dnsight.core`**.

Use the child pages for auto-generated API details from docstrings:

- [dnsight package](dnsight.md) — `run_check_sync`, `run_domain_sync`, check helpers, etc.
- [dnsight.core](core.md) — core types, logging, and submodules referenced from the SDK
